# ============================================================
# SERVIDOR
# ============================================================
server.port=8080

# ============================================================
# BASE DE DATOS - MySQL
# ============================================================
# Antes de ejecutar, crea la BD:  CREATE DATABASE demo_db;
spring.datasource.url=jdbc:mysql://localhost:3306/demo_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Si usas PostgreSQL en vez de MySQL, comenta lo de arriba y descomenta esto:
# spring.datasource.url=jdbc:postgresql://localhost:5432/demo_db
# spring.datasource.username=postgres
# spring.datasource.password=TU_PASSWORD_AQUI
# spring.datasource.driver-class-name=org.postgresql.Driver

# ============================================================
# JPA / HIBERNATE
# ============================================================
# "update" = crea/modifica tablas automaticamente segun tus @Entity
# En produccion usa "validate" o migraciones con Flyway/Liquibase
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
# Para PostgreSQL: org.hibernate.dialect.PostgreSQLDialect

# ============================================================
# JWT
# ============================================================
# Clave secreta para firmar tokens (minimo 256 bits / 32 caracteres)
# NUNCA subas esto a Git. Usa variables de entorno en produccion.
jwt.secret=MiClaveSecretaSuperSeguraQueDebeSerMuyLarga2025!
jwt.expiration=86400000
# 86400000 ms = 24 horas

# ============================================================
# EMAIL (para recuperacion de contrasena)
# ============================================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=trujillobrayhan17@gmail.com
spring.mail.password=ykrb qdzg okpk zual
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# ============================================================
# SWAGGER / OPENAPI
# ============================================================
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# ============================================================
# LOGGING (opcional, util para debug)
# ============================================================
logging.level.org.springframework.security=DEBUG
logging.level.com.example.demo=DEBUG